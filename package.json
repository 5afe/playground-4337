{
  "name": "playground-4337",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "fmt": "prettier -w . && prettier -w --plugin prettier-plugin-solidity 'contracts/**/*.sol'",
    "playground": "hardhat run --network geth scripts/playground.js",
    "setup": "hardhat run --network geth scripts/setup.js",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@account-abstraction/contracts": "^0.6.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0",
    "prettier": "^3.0.3",
    "prettier-plugin-solidity": "^1.2.0"
  }
}
